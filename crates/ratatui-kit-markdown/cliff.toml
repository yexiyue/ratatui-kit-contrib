# git-cliff configuration ~ https://git-cliff.org/docs/configuration
#
# This crate is released from the `ratatui-kit-contrib` monorepo with a per-crate
# tag `ratatui-kit-markdown-v<version>` (e.g. `ratatui-kit-markdown-v0.1.0`).
# Generate/refresh the changelog scoped to this crate with:
#
#   git cliff --tag-pattern '^ratatui-kit-markdown-v[0-9].*' \
#             --include-path 'crates/ratatui-kit-markdown/**' \
#             -o crates/ratatui-kit-markdown/CHANGELOG.md

[changelog]
# Tera template rendered per release (keep-a-changelog style).
# `version` looks like `ratatui-kit-markdown-v0.1.0`; split on `-v`, take the last
# segment and drop a leftover `v`, so it renders as `## [0.1.0]`.
body = """
{% if version %}\
    ## [{{ version | split(pat="-v") | last | trim_start_matches(pat="v") }}] - {{ timestamp | date(format="%Y-%m-%d") }}
{% else %}\
    ## [unreleased]
{% endif %}\
{% for group, commits in commits | group_by(attribute="group") %}
    ### {{ group | striptags | trim | upper_first }}
    {% for commit in commits %}
        - {% if commit.scope %}*({{ commit.scope }})* {% endif %}\
            {% if commit.breaking %}[**breaking**] {% endif %}\
            {{ commit.message | upper_first }}\
    {% endfor %}
{% endfor %}
"""
trim = true
render_always = true
postprocessors = []

[git]
conventional_commits = true
filter_unconventional = true
require_conventional = false
split_commits = false
commit_preprocessors = []
protect_breaking_commits = false
commit_parsers = [
    { message = "^feat", group = "<!-- 0 -->🚀 Features" },
    { message = "^fix", group = "<!-- 1 -->🐛 Bug Fixes" },
    { message = "^doc", group = "<!-- 3 -->📚 Documentation" },
    { message = "^perf", group = "<!-- 4 -->⚡ Performance" },
    { message = "^refactor", group = "<!-- 2 -->🚜 Refactor" },
    { message = "^style", group = "<!-- 5 -->🎨 Styling" },
    { message = "^test", group = "<!-- 6 -->🧪 Testing" },
    { message = "^chore\\(release\\): prepare for", skip = true },
    { message = "^chore\\(release\\):", skip = true },
    { message = "^chore\\(deps.*\\)", skip = true },
    { message = "^chore\\(pr\\)", skip = true },
    { message = "^chore\\(pull\\)", skip = true },
    { message = "^chore|^ci", group = "<!-- 7 -->⚙️ Miscellaneous Tasks" },
    { body = ".*security", group = "<!-- 8 -->🛡️ Security" },
    { message = "^revert", group = "<!-- 9 -->◀️ Revert" },
    { message = ".*", group = "<!-- 10 -->💼 Other" },
]
filter_commits = false
fail_on_unmatched_commit = false
link_parsers = []
# Only treat this crate's release tags as release boundaries.
tag_pattern = "^ratatui-kit-markdown-v[0-9].*"
use_branch_tags = false
topo_order = false
topo_order_commits = true
sort_commits = "oldest"
recurse_submodules = false
